# TrackX Backend Environment (MongoDB)
# Use MongoDB Atlas free tier or local replica set
# For MongoDB Atlas, use: mongodb+srv://username:password@cluster.mongodb.net/trackx
# For local replica set, ensure MongoDB is running with --replSet rs0

DATABASE_URL="mongodb://localhost:27017/trackx?directConnection=true"
SKIP_REDIS="true"
JWT_ACCESS_SECRET="trackx-dev-access-secret-key-32chars-minimum"
JWT_REFRESH_SECRET="trackx-dev-refresh-secret-key-32chars-minimum"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"
PORT=3001
NODE_ENV="development"
CORS_ORIGIN="http://localhost:5173"
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
GPS_OFFLINE_THRESHOLD_SECONDS=120
MAX_FILE_SIZE_MB=5
UPLOAD_DIR="./uploads"
